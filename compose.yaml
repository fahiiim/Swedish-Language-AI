services:
  api:
    build:
      context: .
    env_file:
      - .env
    environment:
      APP_ENV: development
      DOCS_ENABLED: "true"
      PORT: "8000"
    ports:
      - "8000:8000"
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    init: true
    restart: unless-stopped
